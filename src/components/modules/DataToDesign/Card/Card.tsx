import { MTGCardProps } from '@/types/types';
import { Box } from '@mui/material';

interface CardProps{
  mtgData: MTGCardProps[]
}

const Card = ({ mtgData }: CardProps) => {
  


  return(
    <Box sx={{  }}>
    
    </Box>
  )
}

export default Card;

export interface ProjectsProps {
  name          : string;
  desc          : string;
  ribbon        : string;
  url           : string;
  github        : string;
  status        ?: string;
  referenceLink ?: string;
}